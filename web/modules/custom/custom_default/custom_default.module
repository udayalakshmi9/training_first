<?php
/**
 * Implements hook_theme().
 */
function custom_default_theme($existing, $type, $theme, $path) {
  return [
    'article_list' => [
      'variables' => array('items' => array(),'title'=>''),
    ],
  ];
}
